{
  "name": "cash-program-design-wizard-server",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rodekruis/cash-program-design-wizard.git",
    "directory": "server"
  },
  "scripts": {
    "lint": "npx prettier --check .",
    "test": "npm run lint",
    "watch:test": "echo 'No watcher(s) specified yet.' && exit 0"
  }
}
