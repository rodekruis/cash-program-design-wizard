import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cash-program-design-wizard',
  appName: 'cash-program-design-wizard',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
